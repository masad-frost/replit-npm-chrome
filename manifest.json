{
  "manifest_version": 2,
 
  "name": "NPM + Repl.it",
  "version": "0.0.1",
  "description": "Adds a try it on repl.it link to npm package pages",
 
  "content_scripts": [{
    "css": ["styles.css"],
    "js": ["index.js"],
    "matches": ["https://www.npmjs.com/package/*"]
  }]
}